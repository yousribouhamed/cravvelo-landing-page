"use client";

import type { FC } from "react";

const TextTyper: FC = ({}) => {
  return (
    <span
      className="text-[#FC6B00] font-bold text-[1em] leading-none min-w-[10px] h-fit w-fit mt-[10px] sm:mt-[0] mx-2 border-[#FC6B00] border-2 rounded-full block sm:inline-block bg-[#F8FAE5] px-3 py-2.5"
      // style={{ fontFamily: "Qatar2022 Arabic bold" }}
    >
      your digital products
    </span>
  );
};

export default TextTyper;
